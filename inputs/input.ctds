Program 
{
    integer y = 5+5;

    integer get_int(integer aa, integer bb) extern ;
    
    integer method1() 
    {
        while (true || false == (get_int() < 1)) {

        }
        return 1;
    }

    void holaMundo() {
        return;
    }

    void main()
    {
        
        integer x = get_int();

        if(y == 1)then{

        }
        return;
    }
}

// bool x = 1 < get_int(); no funca esto
// bool x =  < 1; no funca esto