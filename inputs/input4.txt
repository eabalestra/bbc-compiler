Program {
    bool x = true;

    bool get_value() {
        return false;
    }

    void process_value(integer n) {
        if (x) then {
            x = false;
        } else {
            x = true;
        }
        return;
    }

    void main() {
        bool val = get_value();
        process_value(1);
        return;
    }
}