Program {
   
    integer count = 0;
    bool terminated = false;

    void printInteger(integer x) extern;

    void main() {
        while (count < 10 && !terminated)
        {
            printInteger(count);
            count = count + 1;
        }
    }
}