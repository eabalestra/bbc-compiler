Program {
    /*
        Example to test while loops
    */
    
    integer count = 0;
    bool flag = true;
    
    void main() {
        while (count < 10) {
            count = count + 1;
        }
        if (flag && count == 0) then {
            count = 1;
        }
        return;
    }
}