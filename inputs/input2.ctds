Program {
    /*
        Example to test while loops
    */
    
    integer count = 0;
    bool flag = true;
    
    integer inc(integer x)
    {
        integer y;
        return x + x;
    }

    void main() {
        integer x = 1;
        while (count < 10) {
            count = count + 1;
        }
        if (flag && count == 0) then {
            count = 1;
        }
        return;
    }
}